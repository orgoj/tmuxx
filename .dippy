allow-edit ./**

# task manager dots
deny rm .dots/* "For dot task use only dot cli!"
deny-read .dots/* "For dot task use only dot cli!"
deny-write .dots/* "For dot task use only dot cli!"
deny-redirect .dots/* "For dot task use only dot cli!"
deny cat .dots/** * "For dot task use only dot cli!"
allow dot *


deny scripts/cp-bin.sh * "User does this when application works!"
allow ./scripts/reload-test.sh

allow tmuxx test *

allow cargo build *
allow cargo clippy *
allow cargo fmt *
allow cargo run -- test *
allow cargo test *

allow cargo +stable build *
allow cargo +stable clippy *
allow cargo +stable fmt *
allow cargo +stable run -- test *
allow cargo +stable test *

allow touch src/**

allow tmux list-sessions *
allow tmux list-panes *
allow tmux list-windows *
allow tmux has-session *

allow tmux capture-pane *
# allow tmux send-keys *
allow tmux display-message *

allow tmux capture-pane -t ct-test *
allow tmux send-keys -t ct-test *
allow tmux display-message -t ct-test *

allow-redirect ./tmp/*

allow git add *

# docasne pro reorganizaci
#allow cp *

# yolo
allow git commit *
allow git add *
allow rustfmt *
